pipeline {
    agent any
    environment {
        DOCKER_HUB_USER = 'mohamedsaidneffati'
        CREDENTIALS_ID = 'dockerhub-credentials'
        IMAGE_CLUBS = "${DOCKER_HUB_USER}/linguanova-clubs-service"
        IMAGE_TAG = "1.0.${BUILD_NUMBER}"
    }
    stages {
        stage('CD: Push Clubs Service to Docker Hub') {
            steps {
                withCredentials([usernamePassword(credentialsId: env.CREDENTIALS_ID, passwordVariable: 'DOCKER_PASS', usernameVariable: 'DOCKER_USER')]) {
                    sh 'echo "$DOCKER_PASS" | docker login -u "$DOCKER_USER" --password-stdin'

                    dir('backend/microservices/clubs-service') {
                        sh "docker build -t ${IMAGE_CLUBS}:${IMAGE_TAG} -t ${IMAGE_CLUBS}:latest ."
                        sh "docker push ${IMAGE_CLUBS}:${IMAGE_TAG}"
                        sh "docker push ${IMAGE_CLUBS}:latest"
                    }
                }
            }
        }
    }
    post {
        always {
            sh 'docker logout'
            cleanWs()
        }
    }
}
